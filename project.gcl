@library("std", "8.0.58-dev");      // Standard library (required)
@library("explorer", "8.0.3-dev");   // Graph UI (dev only)

@library("ai", "8.0.0-dev");        // AI/LLM support
@library("algebra", "8.0.0-dev");   // ML and numerical computing
@library("kafka", "8.0.0-dev");     // Kafka integration
@library("sql", "8.0.0-dev");       // PostgreSQL support (postgres library)
@library("finance", "8.0.0-dev");   // Financial utilities
@library("powerflow", "8.0.0-dev"); // Power flow analysis
@library("opcua", "8.0.0-dev");     // OPC UA integration
@library("useragent", "8.0.0-dev"); // User agent parsing

fn main() {
    // s3_example::main();
    // sql_example::main();
    // ai_example::main();
}
